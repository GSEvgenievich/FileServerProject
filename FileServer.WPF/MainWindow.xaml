<Window x:Class="FileServer.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:FileServer.WPF.Converters"
        Title="📁 Файловый сервер" Height="650" Width="950"
        MinHeight="500" MinWidth="800"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">

    <Window.Resources>
        <converters:Base64ToImageConverter x:Key="Base64ToImageConverter"/>
        <converters:BytesToImageConverter x:Key="BytesToImageConverter"/>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ВЕРХНЯЯ ПАНЕЛЬ -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" 
                CornerRadius="8" Padding="15" Margin="10,10,10,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal">
                    <Button Content="📤 Загрузить файл" 
                            Command="{Binding UploadCommand}"
                            IsEnabled="{Binding IsUploading, Converter={StaticResource BoolToVis}, ConverterParameter=Inverse}"
                            Width="140" Height="36" Margin="0,0,10,0"
                            Background="{StaticResource PrimaryBrush}" 
                            Foreground="White" BorderThickness="0" Cursor="Hand"
                            FontWeight="SemiBold">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </Button.Resources>
                    </Button>

                    <Button Content="🔄 Обновить" 
                            Command="{Binding RefreshCommand}"
                            Width="120" Height="36"
                            Background="{StaticResource SecondaryBrush}" 
                            Foreground="White" BorderThickness="0" Cursor="Hand"
                            FontWeight="SemiBold">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>

                <!-- Прогресс загрузки -->
                <StackPanel Grid.Column="1" Margin="20,0,0,0" 
                            Visibility="{Binding IsUploading, Converter={StaticResource BoolToVis}}">
                    <TextBlock Text="Загрузка..." FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <ProgressBar Height="8" Value="{Binding UploadProgress}" 
                                 Minimum="0" Maximum="100"
                                 Foreground="{StaticResource PrimaryBrush}"
                                 Background="#E0E0E0"/>
                    <TextBlock Text="{Binding UploadProgress, StringFormat='{}{0:F0}%'}" 
                               FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" 
                               HorizontalAlignment="Right"/>
                </StackPanel>

                <!-- Счетчик файлов -->
                <Border Grid.Column="2" Background="#E8F4FD" CornerRadius="6" Padding="15,5">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📄 " FontSize="16"/>
                        <TextBlock Text="{Binding Files.Count, StringFormat='Файлов: {0}'}" 
                                   FontWeight="SemiBold" FontSize="14"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- СПИСОК ФАЙЛОВ -->
        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" 
                CornerRadius="8" Margin="10,5,10,5">
            <ListBox x:Name="FileListBox" ItemsSource="{Binding Files}"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     VirtualizingStackPanel.IsVirtualizing="True"
                     VirtualizingStackPanel.VirtualizationMode="Recycling"
                     BorderThickness="0" Background="Transparent">

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#E8E8E8" BorderThickness="0,0,0,1" 
                                Padding="12,10" Background="Transparent">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Превью / Иконка -->
                                <Border Grid.Column="0" Width="60" Height="60" 
                                        Background="#F8F9FA" CornerRadius="8"
                                        VerticalAlignment="Center">
                                    <Grid>
                                        <!-- Иконка для не-изображений -->
                                        <TextBlock Text="{Binding DisplayIcon}" FontSize="32" 
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        
                                        <!-- Изображение (если есть эскиз) -->
                                        <Image Source="{Binding ThumbnailBase64, Converter={StaticResource Base64ToImageConverter}}"
                                               Stretch="Uniform"
                                               Visibility="{Binding IsImage, Converter={StaticResource BoolToVis}}"/>
                                    </Grid>
                                </Border>

                                <!-- Информация о файле -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="15,0">
                                    <TextBlock Text="{Binding FileName}" 
                                               FontWeight="SemiBold" FontSize="14"
                                               TextTrimming="CharacterEllipsis"
                                               Foreground="{StaticResource TextPrimaryBrush}"/>

                                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                        <TextBlock Text="{Binding FormattedSize}" 
                                                   Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                                        <TextBlock Text=" • " Foreground="#999" FontSize="12"/>
                                        <TextBlock Text="{Binding ContentType}" 
                                                   Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                                        <TextBlock Text=" • " Foreground="#999" FontSize="12"/>
                                        <TextBlock Text="{Binding UploadedAt, StringFormat='dd.MM.yyyy HH:mm'}" 
                                                   Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                                    </StackPanel>
                                </StackPanel>

                                <!-- Кнопки действий -->
                                <StackPanel Grid.Column="2" Orientation="Horizontal" 
                                            VerticalAlignment="Center" Margin="10,0">

                                    <!-- Просмотр (только для изображений) -->
                                    <Button Content="👁" 
                                            Command="{Binding DataContext.ViewImageCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                            CommandParameter="{Binding}"
                                            Visibility="{Binding IsImage, Converter={StaticResource BoolToVis}}"
                                            Width="40" Height="40" Margin="0,0,5,0"
                                            ToolTip="Просмотр"
                                            Background="#17A2B8" Foreground="White"
                                            BorderThickness="0" Cursor="Hand">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="6"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>

                                    <!-- Скачать -->
                                    <Button Content="⬇" 
                                            Command="{Binding DataContext.DownloadCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                            CommandParameter="{Binding}"
                                            Width="40" Height="40" Margin="0,0,5,0"
                                            ToolTip="Скачать"
                                            Background="{StaticResource SuccessBrush}" Foreground="White"
                                            BorderThickness="0" Cursor="Hand">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="6"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>

                                    <!-- Удалить -->
                                    <Button Content="🗑" 
                                            Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                            CommandParameter="{Binding}"
                                            Width="40" Height="40"
                                            ToolTip="Удалить"
                                            Background="{StaticResource DangerBrush}" Foreground="White"
                                            BorderThickness="0" Cursor="Hand">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="6"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>

                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
            </ListBox>
        </Border>

        <!-- СТАТУС БАР -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" 
                CornerRadius="8" Padding="12,10" Margin="10,5,10,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="📌 " FontSize="14"/>
                    <TextBlock Text="{Binding StatusMessage}" FontSize="13" 
                               Foreground="{StaticResource TextPrimaryBrush}"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <TextBlock Grid.Column="1" Text="🌐 API: http://localhost:5000" 
                           FontSize="12" Foreground="{StaticResource PrimaryBrush}"/>
            </Grid>
        </Border>

        <!-- ВСПЛЫВАЮЩЕЕ УВЕДОМЛЕНИЕ -->
        <Border HorizontalAlignment="Right" VerticalAlignment="Bottom" 
                Margin="0,0,20,80" CornerRadius="8"
                Background="#333333" Padding="20,12"
                Visibility="{Binding ShowNotification, Converter={StaticResource BoolToVis}}">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
            </Border.Effect>
            <TextBlock Text="{Binding NotificationMessage}" 
                       Foreground="White" FontSize="14" FontWeight="Medium"/>
        </Border>
    </Grid>
</Window>